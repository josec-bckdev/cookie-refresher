# Test environment — safe dummy values for unit/integration tests.
# No real credentials. No real services.
ANTHROPIC_API_KEY=test-key
LOGIN_EMAIL=test@example.com
LOGIN_PASSWORD=test-password
ACTION_SCRIPT_PATH=data/action_script.json
